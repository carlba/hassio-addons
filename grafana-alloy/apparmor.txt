#include <tunables/global>

profile grafana-alloy flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  # Capabilities
  file,
  signal (send) set=(kill,term,int,hup,cont),

  # Bashio
  /run.sh r,
  /usr/lib/bashio/** ix,
  /tmp/** rwk,

  # Access to options.json and other files within your app
  /data/** rw,

  # Start new profile for Alloy
  /usr/bin/alloy cx -> alloy,

  profile alloy flags=(attach_disconnected,mediate_deleted) {
    #include <abstractions/base>

    # Access to options.json and other files within your app
    /data/** rw,

    # Access required for service functionality
    /usr/bin/alloy r,
    /etc/passwd r,

    network,
  }
}
